const FS = require('fs')
/**
 * 测试
 *
 * @param {String} data http请求所携带的信息
 * @param {http.IncomingMessage} req
 * @param {http.ServerResponse} res
 */
  // 获取git传递过来的信息：分支，操作，提交人，commit号，时间
  // 执行脚本
  // 容错处理
function deploy (data, req, res) {
  res.end()
}

module.exports = {
}
